# 🧠 الذاكرة الذكية (Thakira AI)

> **"ذاكرتك الثانية، مدعومة بالذكاء الاصطناعي."**

تطبيق **الذاكرة الذكية** هو تطبيق متطور مبني باستخدام React و TypeScript، يهدف إلى أرشفة ذكريات المستخدم (صوت، فيديو، صور، نصوص) وتحليلها تلقائياً باستخدام نماذج Google Gemini. يعمل التطبيق كـ "دماغ رقمي" يسمح باسترجاع اللحظات، استخراج النصوص، جدولة التذكيرات، والبحث الدلالي الذكي.

---

## ✨ المميزات الرئيسية

*   **🎙️ التقاط متعدد الوسائط:** تسجيل الصوت، الفيديو، التقاط الصور، وتدوين الملاحظات النصية مع معاينة حية.
*   **🤖 تحليل ذكي فوري:** استخدام نموذج **Gemini 2.5 Flash** لتحليل المحتوى تلقائياً (تحويل الصوت لنص، وصف الصور، استخراج الوسوم، التلخيص).
*   **🔍 بحث دلالي (Semantic Search):** لا يبحث التطبيق عن الكلمات فقط، بل يفهم المعنى والسياق للإجابة على أسئلتك (مثل: "ماذا قلت في اجتماع الأسبوع الماضي؟").
*   **⏰ تذكيرات ذكية:** اكتشاف المواعيد تلقائياً من سياق الحديث أو النص، مع إمكانية الربط بتقويم جوجل (Google Calendar) وتنبيهات النظام.
*   **📂 أرشفة وتنظيم:** نظام فلترة متقدم (حسب النوع، التاريخ، المفضلة) مع واجهة عرض لا نهائية (Infinite Scroll) لأداء عالي.
*   **📱 جاهز للجوال:** مبني بتقنية **Capacitor** ليعمل كتطبيق أندرويد (APK) أصلي بكامل الصلاحيات.
*   **💾 يعمل محلياً:** يعتمد على **IndexedDB** لتخزين البيانات والوسائط، مما يضمن الخصوصية والعمل دون اتصال (Offline-first) جزئياً.

---

## 🛠️ التقنيات المستخدمة

تم بناء المشروع باستخدام أحدث التقنيات لضمان الأداء والسرعة:

| التقنية | الوصف |
| :--- | :--- |
| **React 19** | مكتبة الواجهة الأمامية الأساسية. |
| **TypeScript** | لضمان كود قوي وخالٍ من الأخطاء النوعية. |
| **Vite** | أداة بناء وتطوير فائقة السرعة. |
| **Tailwind CSS** | لتصميم واجهات عصرية ومتجاوبة (Mobile-First). |
| **Google GenAI SDK** | التفاعل مع نماذج الذكاء الاصطناعي (Gemini). |
| **IndexedDB** | قاعدة بيانات المتصفح لتخزين الذكريات الضخمة. |
| **Capacitor** | تحويل تطبيق الويب إلى تطبيق أندرويد (Native). |
| **Lucide React** | مكتبة أيقونات عصرية وخفيفة. |

---

## 🚀 دليل التثبيت والتشغيل

اتبع الخطوات التالية لتشغيل المشروع على جهازك:

### المتطلبات المسبقة
*   Node.js (الإصدار 18 أو أحدث).
*   مفتاح API من Google AI Studio (Gemini).
*   Android Studio (إذا كنت ترغب في بناء ملف APK).

### 1. استنساخ المشروع وتثبيت الحزم

```bash
# تثبيت الاعتماديات
npm install
```

### 2. إعداد مفاتيح البيئة
يجب عليك تعيين مفتاح API الخاص بـ Gemini. في بيئة التطوير الحالية، تأكد من وجود `API_KEY` في متغيرات البيئة (`process.env.API_KEY`).

### 3. التشغيل في وضع التطوير (المتصفح)

```bash
npm run dev
```
سيفتح التطبيق على `http://localhost:5173`.

---

## 📱 بناء تطبيق الأندرويد (APK)

لتحويل هذا المشروع إلى تطبيق جوال قابل للتثبيت:

1.  **بناء نسخة الويب:**
    ```bash
    npm run build
    ```

2.  **مزامنة ملفات Capacitor:**
    ```bash
    npx cap add android  # (تنفذ مرة واحدة فقط)
    npx cap sync
    ```

3.  **فتح Android Studio:**
    ```bash
    npx cap open android
    ```
    *   انتظر حتى ينتهي الـ Gradle Sync.
    *   من القائمة العلوية: **Build** > **Build Bundle(s) / APK(s)** > **Build APK(s)**.

---

## 📂 هيكلية المشروع

```
src/
├── components/       # مكونات الواجهة (Capture, Memories, Search)
├── services/         # الخدمات (Database, AI, Calendar)
├── types.ts          # تعريفات الأنواع (TypeScript Interfaces)
├── App.tsx           # المكون الرئيسي وإدارة التنقل
├── index.html        # ملف الدخول وتضمين الخطوط
└── capacitor.config  # إعدادات تطبيق الجوال
```

---

## 🛡️ الخصوصية والأمان

*   **البيانات:** يتم تخزين جميع الصور، التسجيلات، والنصوص محلياً على جهاز المستخدم باستخدام IndexedDB.
*   **الذكاء الاصطناعي:** يتم إرسال البيانات إلى Google Gemini فقط لغرض التحليل في اللحظة الآنية، ولا يتم تخزينها سحابياً من قبل التطبيق.

---

## 🤝 المساهمة

نرحب بمساهماتكم! إذا كان لديك اقتراح لتحسين التطبيق أو إضافة ميزة جديدة:
1.  قم بعمل Fork للمشروع.
2.  أنشئ فرعاً جديداً (`git checkout -b feature/AmazingFeature`).
3.  أرسل طلب دمج (Pull Request).

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر تحت رخصة [MIT](LICENSE).

---

<p align="center">
  صُنع بحب ❤️ وبرمجة ذكية 🧠
</p>